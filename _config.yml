title:            没当过飞蛾 如何能懂扑火多快乐
description:      没当过飞蛾，如何能懂扑火多快乐……
disqus_shortname: ripples-alive
reading_time:     true
words_per_minute: 200
# Your site's domain goes here (eg: //mmistakes.github.io, http://mademistakes.com, etc)
# When testing locally leave blank or use http://localhost:4000
url:

# Owner/author information
owner:
    name:           ripples
    avatar:         avatar.png
    bio:            世间本只有愿不愿意，何来值不值得……
    email:          w@q2zy.com
    # Social networking links used in footer. Update and remove as you like.
    twitter:
    facebook:
    github:         ripples-alive
    stackexchange:
    linkedin:
    instagram:
    flickr:
    tumblr:
    # google plus id, include the '+', eg +mmistakes
    # google_plus:    +ripples.alive

# Background image to be tiled on all pages
background:

# Analytics and webmaster tools stuff goes here
google_analytics:
google_verify:
# https://ssl.bing.com/webmaster/configure/verify/ownership Option 2 content= goes here
bing_verify:

# http://en.wikipedia.org/wiki/List_of_tz_database_time_zones
timezone:    Asia/Shanghai
future:      true
highlighter: rouge
markdown:    kramdown
redcarpet:
    extensions: ["smart", "tables", "no_intra_emphasis", "fenced_code_blocks", "autolink", "strikethrough", "superscript", "with_toc_data"]
plugins:
    - jekyll-sitemap
    - jekyll-paginate
    - jekyll-mentions
    - jekyll-feed
    - jekyll-gist
    - jemoji
sass:
    sass_dir: _sass
    style: compressed

# https://github.com/mojombo/jekyll/wiki/Permalinks
permalink:   /articles/:year/:month/:day/:title/

# Amount of post to show on home page
paginate: 10

# Read more
excerpt_separator: '<p id="read-more-anchor"/>'

kramdown:
    input: GFM
    auto_ids: true
    footnote_nr: 1
    entity_output: as_char
    toc_levels: 1..6
    syntax_highlighter: rouge

    syntax_highlighter_opts:
        tab_width: 4
        bold_every: 10
        css_class: 'highlight'
        span:
            line_numbers: false
        block:
            line_numbers: true
            start_line: 1

# Octopress
# Default extensions
post_ext: md
page_ext: md
# Found in _templates/
post_layout: post
page_layout: page
# Format titles with titlecase?
titlecase: true

include: [".htaccess"]
exclude: ["lib", "config.rb", "Capfile", "config", "Gemfile", "Gemfile.lock", "README.md", "LICENSE", "log", "Rakefile", "Rakefile.rb", "tmp", "less", "*.sublime-project", "*.sublime-workspace", "test", "spec", "Gruntfile.js", "package.json", "node_modules"]
